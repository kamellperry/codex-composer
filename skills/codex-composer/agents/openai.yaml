interface:
  display_name: "Codex Composer"
  short_description: "Use Cursor Composer from Codex"
  brand_color: "#1E293B"
  default_prompt: "Use $codex-composer to ask Composer for UI direction before implementing."
dependencies:
  tools:
    - type: "mcp"
      value: "codex-composer"
      description: "Codex Composer MCP server"
policy:
  allow_implicit_invocation: true
